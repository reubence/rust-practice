// A "vector" is what other langauges call a "list."
// You can very roughly think of "{:?}" as being what 
// you use to print "complex" types that have variable size. 

fn main() {
	let my_vec = vec![1,2,3,4];
    
	println!("{:?}", my_vec);
} 